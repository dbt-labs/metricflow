test_name: test_graph_snapshot
test_filename: test_mf_graph.py
docstring:
  Check the graph snapshot.
---
default:
  FlowGraph(
    nodes={
      SourceNode(node_name='source'),
      IntermediateNode(node_name='a'),
      IntermediateNode(node_name='b'),
      SinkNode(node_name='sink'),
    },
    edges={
      FlowEdge(edge_name='source_to_a'),
      FlowEdge(edge_name='source_to_b'),
      FlowEdge(edge_name='a_to_sink'),
      FlowEdge(edge_name='b_to_sink'),
    },
  )
dot_notation:
  digraph FlowGraph {
  	subgraph cluster_intermediate_nodes {
  		a
  		b
  	}
  	sink
  	source
  	a -> sink
  	b -> sink
  	source -> a
  	source -> b
  }
