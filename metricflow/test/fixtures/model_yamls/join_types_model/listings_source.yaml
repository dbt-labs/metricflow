---
data_source:
  name: listings_source
  description: listings_source

  sql_table: $source_schema.dim_listings_for_joins

  dimensions:
    - name: country
      type: categorical

  entities:
    - name: listing
      type: primary
      expr: listing_id
