---
data_source:
  name: companies
  description: If a user is a company / business, this defines the mapping.

  sql_table: $source_schema.dim_companies

  dimensions:
    - name: company_name
      type: categorical

  entities:
    - name: company
      type: primary
      expr: company_id
    - name: user
      type: unique
      expr: user_id
