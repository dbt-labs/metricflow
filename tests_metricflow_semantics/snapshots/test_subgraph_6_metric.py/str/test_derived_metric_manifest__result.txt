test_name: test_derived_metric_manifest
test_filename: test_subgraph_6_metric.py
docstring:
  Test generation of the metric subgraph using the derived-metric manifest.
expectation_description:
  The graph should show a complex-metric node that has edges to simple-metric nodes.
---
dot_notation:
  digraph {
  	graph [name=MutableSemanticGraph]
  	subgraph cluster_metric {
  		label=metric
  		"ComplexMetric(bookings_per_view)"
  		"SimpleMetric(bookings)"
  		"SimpleMetric(views)"
  	}
  	"ComplexMetric(bookings_per_view)" -> "SimpleMetric(bookings)"
  	"ComplexMetric(bookings_per_view)" -> "SimpleMetric(views)"
  }

pretty_format:
  MutableSemanticGraph(
    nodes={ComplexMetric(bookings_per_view), SimpleMetric(bookings), SimpleMetric(views)},
    edges={
      MetricDefinitionEdge(
        tail_node=ComplexMetric(bookings_per_view),
        head_node=SimpleMetric(bookings),
        recipe_step=AttributeRecipeStep(),
      ),
      MetricDefinitionEdge(
        tail_node=ComplexMetric(bookings_per_view),
        head_node=SimpleMetric(views),
        recipe_step=AttributeRecipeStep(),
      ),
    },
  )
